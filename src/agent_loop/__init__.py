"""Multi-turn agent loop probe. NOT a greencube benchmark.

Tests whether injecting a hand-written correction into the system prompt
of a tool-using ReAct agent reduces hallucination-driven mistakes on
deterministic tasks with fake tools and real OpenAI function-calling.
"""
